# Runner 环境配置（复制为 .env 后生效）

HOST=0.0.0.0
# 监听端口
PORT=3100

# 启动是否打开本地 bridge 页面（http 页签）
OPEN_BRIDGE=true

# 无头模式（服务器/CI 建议 true，本地开发建议 false）
HEADLESS=false
NO_SANDBOX=true

# 用户目录模式：shared | per-run
# - shared：同一时间仅允许单个运行（更稳），并发会返回 409
# - per-run：每次运行独立目录，支持并发
PROFILE_MODE=shared

# 在 per-run 下是否保留用户目录，便于排查
PERSIST_RUN_PROFILE=false

# 结束策略：timeout | idle | triggered
# - timeout：仅按超时关闭（与 timeoutMs 配合）
# - idle：网络空闲 idleMs 即结束
# - triggered：触发后立即结束
FINISH_POLICY=idle

# 空闲阈值（毫秒，仅在 FINISH_POLICY=idle 时生效）
IDLE_MS=3000

# Playwright 调试日志（DEBUG 命名空间）
# 常用：pw:api / pw:browser / pw:protocol
# 关闭：留空
PW_DEBUG=pw:browser
